VAULT_ADDR='https://vault.example.com' VAULT_TOKEN='PUT_TOKEN_HERE' sh -c 'set -e; work="$(pwd)/vault-exporter-run"; rm -rf "$work"; mkdir -p "$work"; curl -L "https://github.com/AnastasiaFakh/vault-exporter/archive/refs/heads/main.tar.gz" -o "$work/vault-exporter.tar.gz"; tar -xzf "$work/vault-exporter.tar.gz" -C "$work"; cd "$work/vault-exporter-main"; python3 -m venv .venv; . .venv/bin/activate; pip install -r requirements.txt; python app.py'
