powershell -NoProfile -ExecutionPolicy Bypass -Command "$ErrorActionPreference='Stop'; $url='https://github.com/AnastasiaFakh/vault-exporter/archive/refs/heads/main.tar.gz'; $work=Join-Path $PWD 'vault-exporter-run'; New-Item -ItemType Directory -Force -Path $work | Out-Null; $archive=Join-Path $work 'vault-exporter.tar.gz'; Invoke-WebRequest -Uri $url -OutFile $archive; tar -xzf $archive -C $work; Set-Location (Join-Path $work 'vault-exporter-main'); py -3 -m venv .venv; .\.venv\Scripts\python.exe -m pip install -r requirements.txt; $env:VAULT_ADDR='https://vault.example.com'; $env:VAULT_TOKEN='PUT_TOKEN_HERE'; .\.venv\Scripts\python.exe app.py"
